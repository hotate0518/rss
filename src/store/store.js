import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);
const state = {
  article:{}
}
const mutations = {}
const actions = {}
export default new Vuex.Store({
  state,
  mutations,
  actions
});
